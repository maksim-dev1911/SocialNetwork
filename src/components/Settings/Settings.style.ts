import {StylesRecord} from "../../interfaces/Styles";

const sx: StylesRecord = {
    avatarContainer: () => ({
        borderColor: "rgb(210, 214, 219)",
        borderRadius: "50%",
        borderStyle: "dashed",
        borderWidth: "1px",
        padding: "4px",
    }),
}

export default sx;